<UserControl x:Class="System_Resource_Monitor.Views.HistoryChartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:System_Resource_Monitor.ViewModels"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:HistoryChartViewModel}">
    <Grid Margin="20,14,20,14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header: section label on the left, inline color-keyed legend on the right. -->
        <Grid Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Style="{StaticResource SectionLabel}"
                       VerticalAlignment="Center"
                       Text="UTILIZATION HISTORY  ·  LAST 60S"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Rectangle Width="10" Height="2" Fill="{StaticResource AccentBrush}"
                           VerticalAlignment="Center"/>
                <TextBlock Style="{StaticResource SectionLabel}" Margin="6,0,16,0" Text="CPU"/>
                <Rectangle Width="10" Height="2" Fill="#5A9FB5" VerticalAlignment="Center"/>
                <TextBlock Style="{StaticResource SectionLabel}" Margin="6,0,0,0" Text="MEMORY"/>
            </StackPanel>
        </Grid>

        <lvc:CartesianChart Grid.Row="1"
                            Background="{StaticResource BgBrush}"
                            Series="{Binding Series}"
                            XAxes="{Binding XAxes}"
                            YAxes="{Binding YAxes}"
                            LegendPosition="Hidden"
                            MinHeight="200"/>
    </Grid>
</UserControl>
