<UserControl x:Class="System_Resource_Monitor.Views.DiskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:System_Resource_Monitor.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:DiskViewModel}">
    <Border Style="{StaticResource KpiTileLast}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Style="{StaticResource SectionLabel}" Text="DISK READ"/>
                <TextBlock Style="{StaticResource KpiNumber}" Margin="0,2,0,0"
                           Text="{Binding ReadMBs, StringFormat={}{0:F1}}"/>
                <TextBlock Style="{StaticResource KpiSubtext}" Margin="0,2,0,0" Text="MB/S"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="20,0,0,0">
                <TextBlock Style="{StaticResource SectionLabel}" Text="DISK WRITE"/>
                <TextBlock Style="{StaticResource KpiNumber}" Margin="0,2,0,0"
                           Text="{Binding WriteMBs, StringFormat={}{0:F1}}"/>
                <TextBlock Style="{StaticResource KpiSubtext}" Margin="0,2,0,0" Text="MB/S"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
