<UserControl x:Class="System_Resource_Monitor.Views.CpuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:System_Resource_Monitor.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:CpuViewModel}">
    <Border Style="{StaticResource Card}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Style="{StaticResource HeadingText}" Text="CPU"/>

            <Grid Grid.Row="1" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ProgressBar Value="{Binding TotalPercent}" Maximum="100"/>
                <TextBlock Grid.Column="1" Margin="12,0,0,0"
                           Text="{Binding TotalPercent, StringFormat={}{0:F1}%}"/>
            </Grid>

            <ItemsControl Grid.Row="2" ItemsSource="{Binding Cores}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="4"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:CoreUsage}">
                        <StackPanel Margin="4">
                            <Grid>
                                <TextBlock Style="{StaticResource MutedText}"
                                           Text="{Binding Index, StringFormat='Core {0}'}"/>
                                <TextBlock HorizontalAlignment="Right" FontSize="11"
                                           Text="{Binding Percent, StringFormat={}{0:F0}%}"/>
                            </Grid>
                            <ProgressBar Value="{Binding Percent}" Maximum="100" Margin="0,2,0,0"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Border>
</UserControl>
