<UserControl x:Class="System_Resource_Monitor.Views.MemoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:System_Resource_Monitor.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:MemoryViewModel}">
    <Border Style="{StaticResource Card}">
        <StackPanel>
            <TextBlock Style="{StaticResource HeadingText}" Text="Memory"/>

            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ProgressBar Value="{Binding UsedPercent}" Maximum="100"/>
                <TextBlock Grid.Column="1" Margin="12,0,0,0"
                           Text="{Binding UsedPercent, StringFormat={}{0:F1}%}"/>
            </Grid>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Style="{StaticResource MutedText}" Text="Used"/>
                    <TextBlock Text="{Binding UsedGb, StringFormat={}{0:F2} GB}"/>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock Style="{StaticResource MutedText}" Text="Available"/>
                    <TextBlock Text="{Binding AvailableGb, StringFormat={}{0:F2} GB}"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Style="{StaticResource MutedText}" Text="Total"/>
                    <TextBlock Text="{Binding TotalGb, StringFormat={}{0:F2} GB}"/>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
