<UserControl x:Class="System_Resource_Monitor.Views.MemoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:System_Resource_Monitor.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:MemoryViewModel}">
    <Border Style="{StaticResource KpiTile}">
        <StackPanel>
            <TextBlock Style="{StaticResource SectionLabel}" Text="MEMORY"/>
            <TextBlock Style="{StaticResource KpiNumber}" Margin="0,2,0,0"
                       Text="{Binding UsedPercent, StringFormat={}{0:F1}%}"/>
            <TextBlock Style="{StaticResource KpiSubtext}" Margin="0,2,0,0">
                <TextBlock.Text>
                    <MultiBinding StringFormat="{}{0:F1} / {1:F1} GB">
                        <Binding Path="UsedGb"/>
                        <Binding Path="TotalGb"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
        </StackPanel>
    </Border>
</UserControl>
